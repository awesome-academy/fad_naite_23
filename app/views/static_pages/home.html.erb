<div class="container">
  <div class="center jumbotron">
    <h1><%= t ".welcome" %></h1>
    <h2>
      <%= t ".para1_html", href: link_to(t(".foodoo"), t("url.main_page")) %>.
    </h2>
    <%= link_to t(".sign_up"), signup_path, class: "btn btn-lg btn-primary" %>
  </div>
  <%= render partial: "shared/product", collection: @products %>
</div>
